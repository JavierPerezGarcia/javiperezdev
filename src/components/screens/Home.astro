---
import { Icon } from "astro-icon/components";
import Separator from "../atoms/Separator.astro";
import techs from '../../data/technologies.json';
import projects from '../../data/projects.json';
import proyectos from '../../data/proyectos.json';

import { getLangFromUrl } from "../../i18n/utils";

const currentLang = getLangFromUrl(Astro.url);
const data = currentLang === "es" ? proyectos : projects;
---

<main>
    <section class="max-w-6xl mx-auto px-4 mt-8 mb-10">
        <div class="title-name font-staatliches mb-6 color-soft-black">
            <div class="flex items-center mb-3">
                <h1 class="text-4xl name-title"><span class="color-soft-grey mr-2.5">HEY, SOY</span>JAVIER PÉREZ</h1><Icon name="mdi:robot-excited" class="w-10 h-10 ml-3 mb-2 robot-icon"/>
            </div>
            <h2 class="text-3xl">Desarrollador web y apasionado del diseño</h2>
        </div>
        <div class="my-description">
            <p class="font-satoshi-medium text-lg">Me gusta salir a hacer vida “outside” ya que disfruto conectar con las personas en un entorno físico además de lo virtual, entre mis hobbies está el patinaje, la bicicleta, el deporte en general. Nunca tuve claro que quería hacer con mi vida, pero siempre tuve una vena creativa. Estudié informática y poco a poco me fui enamorando de ella. A día de hoy sé que me quiero dedicar al front dentro del sector tecnológico.</p>
        </div>
    </section>
    <Separator></Separator>
    <section class="max-w-6xl mx-auto px-4 projects my-10">
        <div class="tech-title mb-8">
            <h2 class="text-3xl font-staatliches mb-8">ÚLTIMOS PROYECTOS</h2>
            <div class="projects grid grid-cols-3 gap-y-10 gap-x-12">
                {data.map((project) => (
                    <div class="project-card stroke-orange-4 bg-white px-4 pb-4 pt-3">
                        <h1 class="font-staatliches text-2xl mb-2">{project.title}</h1>
                        <img src="" alt="" class="w-full h-42"/>
                        <p class="font-satoshi-medium text-base my-4">{project.description}</p>
                        <div class="flex justify-between flex-wrap">
                            {project.techs.map((tech) => (
                                <div class="font-geist-bold border-2 text-sm px-1 py-0.5">{tech}</div>
                            ))}
                        </div>
                    </div>
                ))}
            </div>
        </div>
    </section>
    <Separator></Separator>
    <section class="max-w-6xl mx-auto px-4 my-12">
        <div class="tech-title font-staatliches mb-8">
            <h2 class="text-3xl">TECNOLOGÍAS</h2>
        </div>
        <div class="techs grid grid-cols-5 gap-y-10 gap-x-12">
            {techs.map((tech) => (
                <div class="tech-card flex flex-col items-center stroke-orange-2 px-4 pb-4 pt-6 bg-white color-soft-black">
                    <Icon name={tech.icon} class="text-5xl mb-2"></Icon>
                    <span class="text-lg font-satoshi-bold">{tech.name}</span>
                </div>
            ))}
        </div>
    </section>
    <Separator></Separator>
</main>